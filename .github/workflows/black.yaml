name: black check
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
        with:
          line-length: '120'
          path: '.'
          include: 'apps'
          exclude: '(/*.html|/*.mo|/*.po|/*.png|/*.rst)'
