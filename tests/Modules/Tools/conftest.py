"""
Conftest for tests/Modules/Tools.

No module-level stubs needed here — all tools_* modules either have no
external imports at the top level, or import from Modules.zigateConsts which
is a real file and must NOT be stubbed (other test suites rely on it).
"""
